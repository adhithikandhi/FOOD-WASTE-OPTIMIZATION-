[project]
name = "food-waste-optimization"
version = "0.1.0"
description = "Food waste optimization web app"
dependencies = [
    "Flask==2.3.3",
    "gunicorn==20.1.0"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"